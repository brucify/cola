curl --request POST \
     --url     $HOST/bookings \
     --cert    coke.crt \
     --key     coke.key \
     --header  'Content-Type: application/json' \
     --verbose \
     --insecure \
     -d @- \
<< EOF
{
  "hello": "world"
}
EOF
